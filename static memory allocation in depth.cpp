#include <stdio.h>

//Static memories are those which allocate during compile time
//The memory allocated is fixed during runtime. 
//for example:- Array's, static variables


int main(){
	
	static int name = 3;		//this all are method of static memory allocation
	int arr[5] = {2,3,4,5,6};
	int num = 8;		
	
return 0;	
}